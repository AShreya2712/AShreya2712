def find_max_vote_candidate(votes):
   
    vote_count = {}
    for candidate in votes:
        vote_count[candidate] = vote_count.get(candidate, 0) + 1

    max_votes = max(vote_count.values())

    max_vote_candidates = [candidate for candidate, count in vote_count.items() if count == max_votes]

    max_vote_candidates.sort()

    return max_vote_candidates[0]


def main():
    n = int(input())
    votes = []
    for _ in range(n):
        votes.append(input())

    print(find_max_vote_candidate(votes))


if __name__ == "__main__":
    main()
